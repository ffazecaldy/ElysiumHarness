/**
 * @elysium/tui — terminal UI (extension): REPL loop, streaming renderer.
 */
export { createRepl } from "./repl";
export type { Repl, ReplEvent, ReplOptions } from "./repl";

export const TUI_VERSION = "0.1.0";
