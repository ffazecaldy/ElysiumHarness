/**
 * Offline demo — runs a scripted task end-to-end on the mock provider.
 * Populated in Phase 4 (C5). Kept as a stub so the workspace builds from Phase 1.
 */
async function main(): Promise<void> {
  console.log("elysium demo: placeholder (Phase 4 will wire the real demo)");
}

main().catch((err: unknown) => {
  console.error(err);
  process.exit(1);
});
